Full Stack – Run, Audit & Fix All Errors

Start both the frontend and backend simultaneously, monitor all output, and fix every error found.

Task Details:

Run the frontend and backend servers together using npm run dev from the root

Monitor the terminal output for any startup errors, warnings, or crashes on either side

Open the browser and check the console for any frontend runtime errors, failed network requests, or broken renders

Check the backend logs for any API errors, database connection issues, or unhandled exceptions

Fix all identified errors on both frontend and backend

Re-run after each fix to confirm the error is fully resolved

Ensure the platform runs cleanly end-to-end with no blocking errors in either the terminal or browser consolebg-card