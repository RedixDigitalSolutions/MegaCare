# ── Backend API ────────────────────────────────────────────────────────────────
# URL of the running backend server.
# The Vite dev server proxies /api, /uploads, and /socket.io to this address.
# Leave at the default if both services run locally.
VITE_API_URL=http://localhost:5000
